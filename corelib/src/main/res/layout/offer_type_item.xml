<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <data>
        <variable
            name="viewModel"
            type="dk.foodservicedanmark.sengrosapp.viewmodels.OfferTypeItemViewModel"/>
    </data>

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="30dp"
        android:paddingStart="10dp"
        android:layout_gravity="center">


<!--        android:fontFamily="@font/sf_pro_text_medium"-->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="30dp"
            android:paddingStart="15dp"
            android:paddingEnd="15dp"
            android:gravity="center"
            android:layout_gravity="center"
            android:text="@{viewModel.productGroup.fieldExternalId}"
            android:onClick="@{() -> viewModel.filterOffersByProductGroup()}"
            android:background="@{viewModel.selected ? @drawable/offer_type_rounded_corners : @drawable/offer_type_rounded_corners_white}"
            android:textColor="@{viewModel.selected ? @color/white : @color/black}"
            android:textSize="13sp"
            tools:background="@drawable/offer_type_rounded_corners"
            tools:text="Dinner"
            tools:textColor="@color/white" />
    </LinearLayout>
</layout>
